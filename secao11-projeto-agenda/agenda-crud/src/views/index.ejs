<%- include("includes/head") %> <%- include("includes/navbar") %>

<div class="container">
  <div class="row">
    <div class="col-lg-2"></div>
    <div class="col-lg-8 my-3">
      <h1 class="text-center">Agenda</h1>
      <p class="text-center lead">Contados cadastrados listados abaixo:</p>

      <%- include ("includes/messages") %>

      <div class="responsive-table">
        <table class="table my-3">
          <tr>
            <th>Nome</th>
            <th>Sobrenome</th>
            <th>Telefone</th>
            <th>Email</th>
            <% if(user) { %>
            <th>Ação</th>
            <% } %>
          </tr>
          <tr>
            <td>1</td>
            <td>2</td>
            <td>3</td>
            <td>4</td>
            <% if(user) { %>
            <td>5</td>
            <% } %>
          </tr>
        </table>
      </div>
    </div>
    <div class="col-lg-2"></div>
  </div>
</div>

<%- include("includes/footer") %>
